/**
 * format.js
 *
 * Module for base of other filters
 */
'use strict';

/**
 * Main class
 * @class Filter
 */
class Filter {
    /**
     * Main class method
     * @method execute
     * @param {Transport} transport Transport to use
     * @param {Message} msg Message to format
     */
    execute(transport, msg) { // jshint ignore: line
        return;
    }
}

module.exports = Filter;
